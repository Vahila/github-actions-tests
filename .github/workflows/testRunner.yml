name: Test Runner
on: 
  workflow_run: 
    workflows: ["Set up MATLAB integration test cases"]

jobs:
  test-runner:
    name: Test Runner
    runs-on: ubuntu-latest
    steps:
      - name: Test files running
        run: echo "Running tests"
